# 贡献指南

感谢你考虑为 Paper-Tutor-Skills（学术辅导套件）做贡献。在开始之前，请先阅读本指南。

## 贡献前提

认同 Paper-Tutor-Skills 的核心定位：**辅导，不是代笔**。先读 [README](README.md) 的"✅ 能做的 / ⛔ 不做的"一节——任何贡献都不得突破这条边界（不代生成研究想法、不代跑实验/代码、不编造数据与引用、不端到端无人确认出稿、不做规避 AI 检测、不出具伦理豁免判断、不接收他人在审稿件）。

## clean-room 红线（必读）

本项目为**独立实现**，不得复制任何第三方项目的提示词、文本、代码或文件结构，**只借鉴公开的设计理念**（copilot 边界哲学、诚信门、检查点机制）。

若你的贡献借鉴了任何第三方项目的公开设计理念，**必须在 PR 描述中声明**：

> 借鉴了 [来源与具体理念]，独立实现为 [本项目的具体做法]。

这是 clean-room 证据，事后无法补记。未作声明的借鉴 PR 将被要求补充或拒绝合入。

## 技术约定

- **Python 3.9+**，生产代码（`skills/_shared/paper_shared/`、`skills/paper-doctor/scripts/`）**零第三方依赖**，只用标准库。
- **测试**用 pytest，位于 `tests/` 下。
- **命令命名**：`paper-` 前缀 + 单个学术熟词。
- **语言**：简体中文优先，术语中文为主、英文括注。

## 开发流程

1. Fork 仓库，从 `main` 切出特性分支（`feat-xxx`、`fix-xxx`）。
2. 开发前先读相关目录的 README：
   - 开发 skill：[skills/README.md](skills/README.md)（skill 包标准结构、发布门清单、六条横切要求）。
   - 写测试：[tests/README.md](tests/README.md)。
3. 提交信息用 `<类型>: <中文描述>` 风格（如 `feat: ...`、`fix: ...`、`docs: ...`）。
4. 提交 PR，填写模板（含 clean-room 勾选项）。
5. 每个 skill 发布前须通过发布门，其中"裸模型 vs 带 skill"并排对比的增量须写进对应 skill 的 README——增量讲不清不发布。

## 链接纪律

`docs/`（PRD、spec、ADR、评审记录）为内部设计文档，不随仓库发布。本文件及所有公开文件**不指向 `docs/`**；若需引用内部设计，用纯文字描述（"设计依据见本地 `docs/`，内部文档"），不生成可点击的相对链接。
