# API 响应 fixtures

| 项 | 内容 |
| --- | --- |
| 类型 | 测试语料说明 |
| 日期 | 2026-07-22 |
| 上游 | 数据源模块设计 spec·第 10 节 |

本目录存放各源 API 响应的手工构造最小样例，供 `tests/datasources/test_*.py`
回放测试使用——单元测试永不碰真实网络。

样例是**按 spec 设计意图构造的合理响应形态**，而非真实抓取的快照。真实端点
契约漂移由 `tests/datasources/smoke_real_api.py` 手动冒烟验证（不进默认测试集）。
